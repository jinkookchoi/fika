# 변경 내역

버전은 `Info.plist`의 `CFBundleShortVersionString` 기준. 최신이 위.

## 1.18
- **상시 CPU 점유 해소**: 창이 보이지 않는데도 CPU를 계속 쓰던 문제를 잡았다. 마스코트 컷 이미지와
  집중 집계(오늘/이번 주/이번 달)를 캐시하고, 팝오버는 닫힐 때 뷰를 버리며, 메뉴바 아이콘은 표시 크기
  비트맵으로 미리 구워 실제로 바뀔 때만 교체한다. 메뉴바 애니메이션 주기는 10Hz → 8Hz.
  재시작 직후 기준으로 팝오버 미개봉 11% → 8.8%, **팝오버를 닫은 직후 17.8% → 7.5%**
  (이전에는 한 번 열면 닫아도 안 내려갔다). 개폐를 반복해도 7~8%대로 안정적이다.

  알려진 제약:
  - **외장 모니터 배율**: 메뉴바 프레임을 앱 시작 시점의 주 디스플레이 배율로 미리 굽는다.
    1x 화면에서 켠 뒤 레티나로 옮기면 아이콘이 흐리게 보일 수 있다. 앱을 다시 켜면 해소된다.
  - **SF Symbol 접근성 라벨**: 심볼 캐시 키가 심볼 이름뿐이라, 같은 심볼에서 상태 라벨이 바뀌어도
    처음 생성 시의 accessibilityDescription이 유지된다. (심볼 테마 + VoiceOver 사용 시)
  - **메모리**: 프레임 252장을 비트맵으로 미리 구워 RSS가 5~6MB 늘었다. CPU와 맞바꾼 것.

  미해결: **mach port 누수**는 잡지 못했다. 팝오버 개폐 5회당 +30 수준으로 계속 증가하며,
  뷰 계층을 버려도 그대로다 — 원인은 SwiftUI 뷰 상주가 아니라 NSPopover의 표시 사이클 자체로 보인다.

## 1.17
- **일시정지 다음날 자동 재개**: 일시정지(수동·"오늘은 그만" 공통)를 해제하는 걸 잊어도 날이 바뀐 뒤
  실제 입력이 감지되면 자동으로 새 작업 사이클을 시작. 어떤 일시정지도 하루를 넘기지 않는다.

## 1.16
- **알림 시스템 통합(Notice 파이프라인)**: 토스트 5종(시작·동작·마무리·고정휴식·남은시간)을 한 벌의 카드
  템플릿으로 통일(폭 440/500 고정·높이 내용 가변). 발화는 `post()` 단일 진입점으로 모으고, 겹치면
  우선순위로 판정 — 최대 2장 스택(위가 사라지면 아래가 올라옴), 다 차면 급한 쪽 교체·덜 급한 쪽 대기
  1건(유효기한 지나면 조용히 폐기).
- **예고 배너 규격 통일**: 배너도 토스트와 같은 카드 규격(폭·크림 배경·글로우). 임박 색 테두리는 유지.
  배너가 떠 있으면 토스트가 배너 아래에 쌓여 서로 가리지 않음.
- **알림 카드 겉모습 공통 설정**: 위치·등장 모션·카드 크게·펄스가 모든 알림 카드에 함께 적용
  (알림 탭의 새 "알림 카드 겉모습" 섹션, 기존 설정값 유지).
- **남은 시간 알림 다듬기**: 숫자 강조를 "같은 크기 + 라운디드 볼드 + 주황색"으로(큰 숫자는 과했음),
  커피잔 줄로 남은/지난 시간 시각화(1잔=5분, 긴 세션은 10/15분 단위 자동 — 최대 12잔).
- **연기 횟수 제한(기본 켬·3회)**: 작업 세션당 연기 예산. 배너 버튼 아래 커피잔 쿠폰으로 남은 횟수 표시,
  다 쓰면 연기 버튼이 사라져 반드시 쉬게 됨. 시간 탭에서 토글·횟수(1~10) 조절, 새 작업 시작 시 리셋.
- **(개발)** `FIKA_TIME_SCALE=N` 시간 압축 모드(실제 알림 경로 그대로 N배속 e2e, 세션 기록 안 함),
  `FIKA_TEST_TOAST=<종류>` 알림 재현 + `FIKA_SNAPSHOT_OUT` PNG 스냅샷 훅.

## 1.15
- **고정 휴식 취소**: 고정 휴식 시간대 중 상태 탭의 "지금 작업 시작"으로 오늘 그 시간대를 건너뛰고 작업 시작
  (윈도우가 끝나면 자동 해제 → 내일은 정상).

## 1.14
- **남은 시간 알림 커스터마이즈**: 위치(상단/중앙/우하단)·등장 모션(스프링/슬라이드/바운스)·소리(없음/차임)·
  표시 시간(3~10초)·히어로 숫자·임박 강조 색·카드 크게·유지 중 펄스를 각각 개별 설정으로. 알림 탭에서 조절,
  "남은시간" 테스트 버튼으로 즉시 확인. 소리는 향후 음성(TTS) 확장 여지를 열어 둠.

## 1.13
- **에너지(A-8·C-3)**: App Nap 명시적 차단(`beginActivity`), 엔진 타이머 관용치 0.2,
  메뉴바 갱신을 커피 프레임 재생 중일 때만 10Hz·그 외 1Hz로.
- **설정 실시간 반영(A-7·§5-3)**: 작업 중 알림 주기를 바꿔도 즉시 재정렬(`onScheduleChange` 훅).
- **테스트 도입(§5-4)**: 시간 계산 순수 로직을 `FikaCore.ScheduleMath`로 분리하고 단위 테스트 추가
  (`swift run FikaTests`, XCTest 미사용 — CLT 환경).

## 1.12
- **알림 테스트 버튼(기능1)**: 알림 탭에서 동작/남은시간/마무리 토스트를 즉석에서 띄워봄.
- **알림 이력(기능2)**: 최근 20건(시각·종류·문구)을 알림 탭에 표시 — "안 온 건지 놓친 건지" 자가진단.
- **커피 폴백(C-1)**: 영상 프레임이 없을 때 절차적 커피잔으로 폴백(투명 아이콘 방지).
- **로그 회전(C-2)**: 시작 시 1회 → 200회 기록마다.

## 1.11
- **자리 비움 복귀 규칙 통일(B-4)**: 비운 시간이 휴식 시간 이상일 때만 리셋, 미만이면 이어감(sleep과 동일).

## 1.10
- **긴급 수정**: 일시정지/재개가 먹통이던 회귀(1.9의 `togglePause` 실수) 복구.
- **실제 집중 시간 기록(B-1·B-2)**: 세션 기록을 "계획된 길이"가 아니라 실제 집중분으로.
  60초 미만 세션(수동 "지금 휴식" 등)은 기록·카운트 제외.

## 1.9
- **뜬금없는 동작 알림 방지(A-6)**: 재개·짧은 sleep 직후 `nextMicroBreak` 재정렬.

## 1.8
- **하루 마무리 알림 독립(A-5)**: 일시정지·고정 휴식 중에도 마무리 알림을 판정.
- **다음날 자동 재개(A-4)**: "오늘은 그만" 후 날이 바뀌고 복귀하면 스스로 재개.

## 1.7
- **타이머 불사화(A-1)**: sleep 시 타이머를 죽이지 않고 tick 간격(gap)으로 보정 — wake 알림 유실에도 안 멈춤.
- **토스트 화면(A-2)**: 토스트·예고 배너를 마우스 커서가 있는 화면에 표시(외장 모니터 대응).
- **연기 재정렬(A-3)**: snooze 후 알림 스케줄을 새 남은 시간에 맞춰 재정렬.

## 1.6
- **휴식 중 sleep 타이머 멈춤 수정**: 휴식은 실제 시간이 흐르게(자는 동안 휴식이 지나감).
- **남은 시간 알림 개선**: 휴식 기준 정렬(…15,10,5분), 표시 분 반올림, 마지막 "곧 휴식" 알림 추가.
